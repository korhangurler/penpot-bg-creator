services:
  penpot-random-background:
    image: nginx:alpine
    container_name: penpot-random-background
    restart: unless-stopped
    ports:
      - "192.168.1.100:4400:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
